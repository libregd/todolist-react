import React from 'react';
import ReactDOM from 'react-dom';
import TodoList from './TodoList';

// through reactDOM put js file to html node
ReactDOM.render(<TodoList />, document.getElementById('root'));